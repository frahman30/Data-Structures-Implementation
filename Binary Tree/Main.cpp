#include <iostream>
#include <conio.h>
#include "Node.h"
#include "Tree.h"
using namespace std;

int main()
{
    Tree T1;
    T1.Insert(10);
    T1.Insert(20);
    T1.Insert(65);
    T1.Insert(75);
    T1.Insert(5);
    T1.Insert(90);
    getch();
    return 0;
}
